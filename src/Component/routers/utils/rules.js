export const rules = {
    location: [
      {
        required: true,
        message: 'Please select location!'
      }
    ],
    zip: [
      ({ getFieldValue }) => ({
      })
    ],
    contact_phone: [
      ({ getFieldValue }) => ({
      })
    ],
  };